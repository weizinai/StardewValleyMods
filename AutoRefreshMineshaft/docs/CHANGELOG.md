# Changelog

# [Unreleased] 0.2.1

- Aligned with the official GMCM 1.16 API and its 1.16.0 minimum version; warnings now show the mod's own name
- Migrated the config menu to the PiCore `ConfigService`/descriptor config module (static `ModConfig.Instance`); no gameplay change