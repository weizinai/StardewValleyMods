# 更新日志

# [待定]

## 变更

- 日志系统迁移至 PiCore（移除 Common 共享项目），控制台日志仍显示各模组名
- Harmony 补丁迁移至 PiCore `Patcher` API（单行 `Patch<T>` 绑定），游戏行为不变
- 配置菜单迁移至 PiCore `ConfigService`/描述器配置模块（静态 `ModConfig.Instance`，损坏配置自愈，菜单页枚举选项改走模块本地化枚举选项），游戏行为不变
- 游戏内菜单迁移至 PiCore UI 框架：菜单支持手柄导航，页签改为顶部横排，翻页改为底部按钮条；为适配 720p 视口，艺术图格子略微缩小。页签、每页九宫格内容与收藏行为不变
- 恢复城镇页签的社区中心选项并重做：点击后从任意地点打开原版 bundle 菜单——与背包页社区中心图标同款入口，自动选中首个仍有束待完成的房间并带房间切换箭头；Joja 路线与未触发森林视野剧情前仍不显示该选项
- 目录行 id `HarveyOption`/`ForgeOption` 去掉 `Option` 后缀改名为 `Harvey`/`Forge`（与其余选项 id 命名对齐）；这两项 id 会写入 `config.json`，已有收藏或默认页签需重新设置